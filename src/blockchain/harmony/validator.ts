export const validateNewContract = async __swap => {
    return true;
};

export const validateWithdraw = async __withdraw => {
    //If the withdraw event is triggered the withdraw is valid, No validation logic is needed
    return true;
};
